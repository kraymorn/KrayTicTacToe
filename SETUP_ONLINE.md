# Пошаговая инструкция: Настройка онлайн игры

## ✅ Шаг 1: Realtime Database создана (выполнено)

Вы уже создали Realtime Database в test mode. Отлично!

## 📋 Шаг 2: Получение конфигурации Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект `alenkraytictactoe`
3. Нажмите на иконку ⚙️ (Settings) рядом с "Project Overview"
4. Выберите "Project settings"
5. Прокрутите вниз до раздела "Your apps"
6. Если у вас еще нет веб-приложения:

   - Нажмите на иконку `</>` (Add app) → Web
   - Зарегистрируйте приложение (можно любое имя, например "megatictactoe")
   - Скопируйте конфигурацию

7. Скопируйте следующие значения из конфигурации:
   - `apiKey` - API ключ
   - `authDomain` - домен аутентификации
   - `databaseURL` - URL базы данных (должен быть что-то вроде `https://alenkraytictactoe-default-rtdb.firebaseio.com`)
   - `projectId` - ID проекта (скорее всего `alenkraytictactoe`)
   - `storageBucket` - storage bucket
   - `messagingSenderId` - sender ID
   - `appId` - App ID

## 🔧 Шаг 3: Создание файла .env

1. В корне проекта создайте файл `.env` (если его еще нет)
2. Добавьте следующие строки, заменив значения на реальные из шага 2:

```env
VITE_FIREBASE_API_KEY=ваш-api-key-здесь
VITE_FIREBASE_AUTH_DOMAIN=alenkraytictactoe.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://alenkraytictactoe-default-rtdb.firebaseio.com
VITE_FIREBASE_STORAGE_BUCKET=alenkraytictactoe.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=ваш-sender-id-здесь
VITE_FIREBASE_APP_ID=ваш-app-id-здесь
```

**Важно:**

- Не используйте кавычки вокруг значений
- Не добавляйте пробелы вокруг знака `=`
- Файл `.env` должен быть в корне проекта (рядом с `package.json`)

## 🔒 Шаг 4: Проверка правил безопасности Realtime Database

1. В Firebase Console перейдите в раздел "Realtime Database"
2. Откройте вкладку "Rules" (Правила)
3. Убедитесь, что правила установлены в "Test mode" (для разработки):

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

4. Нажмите "Publish" (Опубликовать)

**⚠️ Внимание:** Test mode позволяет любому читать и писать в базу данных. Для продакшена нужно настроить более строгие правила.

## 🚀 Шаг 5: Запуск приложения

1. Остановите dev-сервер, если он запущен (Ctrl+C)
2. Перезапустите dev-сервер:

```bash
npm run dev
```

3. Откройте браузер и перейдите по адресу, который покажет Vite (обычно `http://localhost:5173`)

## 🎮 Шаг 6: Тестирование онлайн игры

### Тест 1: Создание комнаты

1. В игре выберите режим **"🌐 Игра по сети"**
2. Нажмите **"Создать комнату"**
3. Должно появиться окно с ID комнаты (например, `ABC123`)
4. Нажмите **"Копировать ссылку"**
5. Скопируйте ссылку (она будет выглядеть как `http://localhost:5173/?room=ABC123`)

### Тест 2: Присоединение к комнате

**Вариант А: По ссылке**

1. Откройте новую вкладку браузера (или другой браузер)
2. Вставьте скопированную ссылку в адресную строку
3. Должно произойти автоматическое присоединение к комнате

**Вариант Б: По ID**

1. В новой вкладке выберите **"🌐 Игра по сети"**
2. Нажмите **"Присоединиться к комнате"**
3. Введите ID комнаты (например, `ABC123`)
4. Нажмите **"Присоединиться"**

### Тест 3: Игра

1. После присоединения второго игрока игра должна начаться автоматически
2. Попробуйте сделать ход - он должен синхронизироваться на обоих экранах
3. Проверьте, что ходы отображаются в реальном времени

## 🐛 Решение проблем

### Проблема: "Ошибка создания комнаты" или "Ошибка присоединения"

**Решение:**

1. Проверьте, что все переменные в `.env` заполнены правильно
2. Убедитесь, что Realtime Database создана и активна
3. Проверьте правила безопасности (должны быть в test mode)
4. Откройте консоль браузера (F12) и проверьте ошибки
5. Перезапустите dev-сервер после изменения `.env`

### Проблема: "Комната не найдена"

**Решение:**

1. Убедитесь, что ID комнаты введен правильно (регистр не важен, но лучше использовать заглавные)
2. Проверьте, что комната еще существует (не была удалена)
3. Попробуйте создать новую комнату

### Проблема: Ходы не синхронизируются

**Решение:**

1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что оба игрока подключены к одной комнате
3. Проверьте правила безопасности Realtime Database
4. Убедитесь, что `databaseURL` в `.env` правильный

### Проблема: Переменные окружения не загружаются

**Решение:**

1. Убедитесь, что файл называется именно `.env` (с точкой в начале)
2. Перезапустите dev-сервер после создания/изменения `.env`
3. Проверьте, что переменные начинаются с `VITE_`
4. Убедитесь, что нет пробелов вокруг знака `=`

## 📝 Проверочный список

- [ ] Realtime Database создана в test mode
- [ ] Конфигурация Firebase получена из Console
- [ ] Файл `.env` создан в корне проекта
- [ ] Все переменные окружения заполнены
- [ ] Правила безопасности установлены в test mode
- [ ] Dev-сервер перезапущен после создания `.env`
- [ ] Тест создания комнаты прошел успешно
- [ ] Тест присоединения по ссылке прошел успешно
- [ ] Ходы синхронизируются между игроками

## 🎯 Готово!

Если все шаги выполнены и тесты прошли успешно, вы можете играть по сети!

**Совет:** Для игры с другом на другом устройстве:

1. Создайте комнату
2. Скопируйте ссылку
3. Отправьте ссылку другу (замените `localhost:5173` на ваш реальный URL при деплое)
4. Друг переходит по ссылке и автоматически присоединяется к игре
